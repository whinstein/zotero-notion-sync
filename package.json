{
  "name": "zotero-notion-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Incremental one-way sync from a Zotero library to a Notion database.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "sync": "node src/sync.js",
    "sync:full": "node src/sync.js --full",
    "sync:dry": "node src/sync.js --dry-run",
    "test": "node --test test/"
  }
}
